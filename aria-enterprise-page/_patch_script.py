# Patch script artifact — do NOT copy this file into the repo.
# This was the helper script used to apply the nav + footer + ribbon changes
# to the 69 modified .html files in this staging folder. The diffs are already
# baked into those .html files. Safe to ignore or delete locally before committing.
